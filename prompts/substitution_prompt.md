Generate exactly three useful ingredient substitutions for the recipe.

Each substitution should name the ingredient being replaced, the replacement, and the reason or dietary benefit. Avoid changing the identity of the recipe unless the user asks for it.

Return valid JSON only:
["substitution 1", "substitution 2", "substitution 3"]
